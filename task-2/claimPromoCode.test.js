import claimPromoCode from './claimPromoCode';
import nock from 'nock';

describe('claimPromoCode', () => {
  it('returns true if the promo code was successfully claimed', () => {

  });

  it('returns false if the promo code was rejected', () => {

  });
});